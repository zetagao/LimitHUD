# Build output
.build/
*.app
*.zip

# Generated icon intermediate (AppIcon.icns is committed for convenience)
AppIcon.iconset/

# macOS
.DS_Store

# Swift Package Manager
Package.resolved
.swiftpm/

# Signing / secrets (never commit keys or certs)
*.p12
*.pem
*.key
CLAUDE.md
design/*.png
tools/petpreview*.swift

# Pet illustration assets are large (122M w/ source video) build inputs — the
# signed release carries them; source builds fall back to the procedural pet.
Assets/

# Local scratch
.vscode/
LimitHUD_FULL_PACKAGE_chatGPT/
